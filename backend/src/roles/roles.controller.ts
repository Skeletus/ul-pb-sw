import { Body, Controller, Get, Param, ParseIntPipe, Post, Req, UseGuards } from '@nestjs/common';
import { Request } from 'express';
import { Roles } from '../common/decorators/roles.decorator'; import { JwtAuthGuard } from '../common/guards/jwt-auth.guard'; import { RolesGuard } from '../common/guards/roles.guard'; import { RolesService } from './roles.service';
@Controller() @UseGuards(JwtAuthGuard,RolesGuard) @Roles('ADMINISTRATOR') export class RolesController { constructor(private readonly service:RolesService){} @Get('roles') roles(){return this.service.roles()} @Get('permissions') permissions(){return this.service.permissions()} @Get('users') users(){return this.service.users()} @Post('users/:id/role') assign(@Param('id',ParseIntPipe)id:number,@Body() body:{roleId:number},@Req() request:Request & {user:{id:number}}){return this.service.assignRole(id,body.roleId,request.user.id)} }
